# Decisions log

Append-only. Numbered, dated, one line each. Newest at the bottom.
This file is authoritative: where any session's reasoning conflicts with an
entry here, the entry wins. Superseded entries are marked SUPERSEDED BY Dnn
rather than deleted.

Format: `Dnn | YYYY-MM-DD | who | decision | rationale (one clause)`

---

D01 | 2026-08-20 | OR | Audio encoding only this study; vision kept in mind for future | scope deliverable in the time
D02 | 2026-08-20 | OR | Speaker identification removed as a probe task | not a target of the multimodal dataset
D03 | 2026-08-20 | SD+Claude | T2 becomes F0 contour estimation, absorbing the prosody task | probes the same source-retention property, needs no labels, permits speaker-disjoint splits
D04 | 2026-08-20 | OR | Target venue: Neuromorphic Computing and Engineering (IOP) | fit and open access
D05 | 2026-08-20 | OR | Channel count is a free swept parameter, not fixed to 700 | no obligation to match SHD tooling
D06 | 2026-08-20 | OR | Learned/task-trained encoders excluded | would bake a task into a general-purpose release
D07 | 2026-08-20 | OR | Dataset name: the MANCHESTER Dataset | —
D08 | 2026-08-20 | OR | SpiNNaker energy measurement out of scope; spike-count and information-per-spike metrics in scope now | staging
D09 | 2026-08-20 | OR | Oliver and Simon approach Wijekoon directly re Spiketrum; E7 not implemented from published description | avoid reimplementing a colleague's algorithm badly
D10 | 2026-08-20 | OR | Authors: Simon Davidson, Oliver Rhodes; others added if appropriate | —
D11 | 2026-08-20 | OR | Twelve-week schedule starts now, in parallel with the TIMIT licence question | Oliver away ~2 weeks; no reason to wait
D12 | 2026-08-20 | SD+Claude | Implementation runs in Claude Code on the Linux box; design stays in the Claude.ai project session | box runs continuously, laptop does not
D13 | 2026-08-20 | SD+Claude | Repository is the sole coordination medium; nothing said in either chat is durable unless written to a file here | neither session has memory or can see the other
D14 | 2026-08-20 | SD+Claude | Known-answer tests and SPEC.md authored by the design session; implementation session may not edit them | preserves the independence the validation protocol requires
D15 | 2026-08-20 | SD+Claude | Repo public, data never committed; GitHub Issues used as an async queue for Simon, not as a channel to the design session | design session can be read from but cannot be polled
D16 | 2026-08-20 | SD+Claude | Auto-continuation permitted for mechanical work only, not generative work | validation protocol section 10 caps implementation speed at review speed
D17 | 2026-09-02 | SD+Claude | LIF membrane potential clamped to reset during absolute refractory, drive discarded; refractory fixed at 0.0 for E1/E4 comparison runs | standard formulation with an exact rate ceiling, while zero refractory keeps E1 a clean non-adapting baseline for P-01
D18 | 2026-09-02 | SD+Claude | E2 lattice reference held as an integer index, r = r0 + m*C, not accumulated by repeated addition | float drift would erode the equation (16) bound over long utterances
D19 | 2026-09-02 | SD+Claude | Gammatone group delay uncompensated by default, compensation available as a declared swept option; setting reported in the paper | biologically faithful default, but it biases T3 timing and no test detects it
D20 | 2026-09-02 | Claude (impl remit) | E2 threshold comparison carries a 1e-9 tolerance in lattice units, and outstanding steps are measured as (u-r0)/C - m rather than (u - r0 - m*C)/C | drive landing exactly on a lattice point is routine, and without either measure double-rounding noise decides equation (14) and drops the crest event of every excursion
D21 | 2026-09-02 | SD+Claude | Equation (9) envelope cutoff is channel-relative, f_cut_c = min(f_cut, b_c), fourth order | a subband cannot carry envelope faster than its own bandwidth, and fixed-cutoff carrier leakage would make E1-E4 and E6 partly phase-locking in the channels carrying F0, blurring the E5 contrast that P-03 rests on
D22 | 2026-09-02 | SD+Claude | test_T2_4 builds its own signal with the endpoint forced equal to the first sample | eq (16) pins the net lattice displacement to zero only when the closure is exact; sine_drive samples a half-open interval
D23 | 2026-09-02 | SD+Claude | D20 promoted from implementation decision into SPEC 4.3 | Layer 3 requires an independent reimplementation of E2 compared event for event, which needs the tolerance in the contract
D24 | 2026-09-03 | SD+Claude | compensate_group_delay advances each channel by the summed declared lag of every stage in the envelope path, not the filterbank alone; a stage that cannot declare its lag raises | compensation applied in subbands cannot remove a lag added downstream, and under D21 the envelope lowpass is the larger contributor in exactly the channels where the gammatone delay is worst
D25 | 2026-09-03 | SD+Claude | test_F6 added, measuring onset spread across the bank with and without compensation on both envelope methods | third channel-dependent timing bias in a row that no test detected

D26 | 2026-09-03 | SD+Claude | E3 event rule is the SPEC 4.3 reference-lattice rule applied to d, spacing theta, anchored at d = 0; equation (21) and proposal 5.3 rewritten | no one-event-per-crossing rule can be a rate parameter, since its count is bounded by the number of excursions of d and saturates as theta falls
D27 | 2026-09-03 | SD+Claude | G3 additionally requires the event count to span at least 4x across the declared sweep, not merely to be monotonic | monotonicity is necessary but not sufficient; a candidate giving 52, 52, 52, 35, 0 passes the original test and cannot hit a matched budget
D28 | 2026-09-03 | SD+Claude | Exponential-filter discretisation alpha = exp(-dt/tau) restated in SPEC 1, covering equations (12), (18)-(19), (22)-(23) and (32) | stated in proposal 5.3 but SPEC cites equations by number only, and SPEC is the sole contract a Layer 3 reimplementation works from
D29 | 2026-09-03 | SD+Claude | test_T3_5 and test_T2_6 added; test_T3_1 docstring and the T3 block header corrected | the T3 block pinned the filters and the symmetry but not the event rule, and T3.1's stated ground for discriminating E3 from E2 was false
D30 | 2026-09-03 | Claude (impl remit) | E2 and E3 share one implementation of the SPEC 4.3 reference-lattice rule, parametrised by the anchor r0 | D26's single-factor contrast is a claim about the code as well as the study, and two copies could diverge while each still passed its own test block
D31 | 2026-09-03 | SD+Claude | test_T3_6 added, asserting E3 equals E2 applied to d; authored after the design session had read encoders.py, and marked as such in the file header and its own docstring | D30 made the single-factor contrast true by construction and tested by nothing, and a later un-sharing would break D26's central claim with no test noticing
D32 | 2026-09-03 | SD+Claude | SPEC 4.4 requires TemporalContrast to raise when tau_slow <= tau_fast | the guard was added within implementation remit, but a Layer 3 reimplementation works from SPEC alone and would not have it, and an inverted pair exchanges ON and OFF silently rather than failing
D33 | 2026-09-03 | SD+Claude | test_T3_5 docstring corrected: continuous peak 0.9048013 not 0.9048124, residual at 0.30 s 6.74e-3 not 2.5e-3, t* 3.99186 ms | Q08; both errors were the design session's, neither affected an assertion, and both were in values that exist specifically to be hand-checked by a Layer 3 reimplementer

D34 | 2026-09-04 | SD | Adaptation state `a` keeps decaying through an absolute refractory period and is not incremented within it | equation (23) has no refractory term and the threshold tracks spike history rather than the membrane, so clamping V says nothing about a

D35 | 2026-09-05 | Claude (impl remit) | Every reported number is produced by a script under `scripts/` driven by a committed JSON config under `configs/`, and registered by `spikeenc.provenance.record`, which writes the data file and the manifest entry together and refuses to run unless the script and config are themselves committed and unmodified | a commit hash records provenance only if the tree that produced the number is the tree the hash names, and untracked-file blindness in a plain dirty check would let a script record a result against a commit not containing it
D36 | 2026-09-05 | Claude (impl remit) | The equation (9) lowpass lag declared under D24 is the first moment of the designed digital filter's impulse response, sum(n*h[n])/sum(h[n]) | it is the DC group delay exactly for the filter actually used, avoids the transfer-function conversion SPEC 3 records as unreliable at the low channels' normalised cutoffs, and agrees with the analog Butterworth prototype to within 0.6 per cent so a Layer 3 reimplementation choosing either route lands in the same place
D37 | 2026-09-05 | SD | The implementation session pushes to `origin/main` automatically, without asking, for this project | the repository is the dataflow between the implementation and design sessions rather than work sent out for colleagues to review, so an unpushed commit is invisible to the other half of the project; this is the deliberate exception to the global ask-before-pushing rule, which was in conflict with this repository's own end-of-session instruction to commit and push
D38 | 2026-09-06 | SD+Claude | test_T3_6's and SPEC 4.4's claim that `d[:, 0]` is exactly zero softened to zero in exact arithmetic and within an ulp in doubles | Q09; the two anchors genuinely differ about 3 per cent of the time, which makes anchoring E3's lattice at exactly zero substantive rather than aesthetic
D39 | 2026-09-06 | SD+Claude | test_T4_3 replaced: the first-spike latency after silence is invariant in delta_a and equals the equation (12) closed form, and the steady-state-to-onset ratio grows | Q10; the early/late count ratio is non-monotone in delta_a for any correct ALIF, whereas the onset latency is adaptation-free because a = 0 at the step, and asserting its invariance is what pins D34
D40 | 2026-09-06 | SD+Claude | E5's RATE_PARAM becomes `cycle_divisor`, keeping every k-th gated crossing; `mode="poisson"` gains lambda_max and z_0 but is excluded from section 6.4 and from G3 and G4 | Q11; the threshold spans 1.04x because the count is capped by the carrier's crossing rate, a Poisson process is shift-stationary rather than shift-equivariant and so cannot satisfy G4, and reaching low budgets by cutting channels would remove frequency resolution at the same time
D41 | 2026-09-06 | SD+Claude | E5's internal gating envelope is half-wave rectification then a fourth-order Butterworth at `env_cutoff`, default 100 Hz; the LIF fallback above f_lock is an E1 instance with section 4.2 defaults on that envelope | Q12; Hilbert magnitude is non-causal and would leak post-boundary information into the pre-boundary gate for one encoder out of six in a battery whose T3 probe is boundary detection, and specifying the fallback as an instance makes the reversion a testable identity
D42 | 2026-09-06 | SD+Claude | test_corrupt_delete_retains_expected_fraction asserts the mean retained fraction over twenty seeds in a window 3.2 standard errors wide, guard lowered to 200 | Q13; the test is named for a rate and twenty draws measure the rate, where lengthening the drive only makes one draw more reliable; the cost is a statistical assertion in a known-answer file, stated in the docstring
D43 | 2026-09-06 | SD+Claude | E6's `e_min` becomes `e_frac`, gating strictly at `e_frac * E_max` with E_max the largest frame energy over all channels and all frames; registry point 0.20 | Q14; an absolute default cannot suit both synthetic drives and real audio and sat 6.8 decades below the quietest frame, a per-channel maximum would destroy the spectral profile, and a non-strict gate emits everywhere on silence
D44 | 2026-09-06 | SD+Claude | E_min in equation (28) is the gate itself; `e_frac <= 0` raises in log mode; E6 exposes `energy` and `offsets` state matrices and test_T6_1/test_T6_2 assert on those rather than on time windows | Q16; the two roles of E_min collapse to one positive quantity, the strict gate places every offset strictly inside its frame without a clipping rule, and frame membership is not recoverable from event times when hop < frame
D45 | 2026-09-06 | SD+Claude | The Q03 leakage figures are corrected by a dated note leaving the original visible, and section 8 of the validation protocol requires every reported figure to carry the definition of what was measured | Q15; all three discrepancies were definitional rather than substantive, and all three would have been impossible had the metric been written beside the value
D46 | 2026-09-06 | SD+Claude | test_G3's docstring corrected: E5, not E6, is the encoder whose event count is structurally bounded by a property of the drive | Q11 and Q14 between them show the prediction in that docstring was wrong on both halves

D47 | 2026-09-07 | Claude (impl remit) | The probe harness is written against a corpus interface, with a synthetic stand-in whose phone labels, boundaries and f0 are generated rather than annotated | TIMIT is blocked on O2, and a corpus with a known answer is the only thing that separates a low score caused by the encoder from one caused by a mislabelled frame, which no real corpus can do because on a real corpus every number is plausible
D48 | 2026-09-07 | Claude (impl remit) | C4 is enforced in `splits.Split.__post_init__`, so a leaking split cannot be constructed; C2, C3, C5 and C6 are computed by `run_t1` on every call with no argument to disable them | a control that has to be remembered is one that will one day be forgotten, and the run it is forgotten on is by construction the one nobody was watching
D49 | 2026-09-07 | Claude (impl remit) | The linear probe of proposal 6.2 is implemented in numpy and scipy rather than taken from scikit-learn, and validated against scikit-learn out of tree | CI installs `.[dev]` and runs every file in `tests/`, and the workflow is a design-session file this session may not edit, so a probe needing scikit-learn would make its own tests unrunnable in the one environment that checks them independently; agreement measured at 8.2e-06 max absolute coefficient difference and exact on predictions
D50 | 2026-09-07 | Claude (impl remit) | Sweeps are specified as target event rates, with the rate parameter obtained by bisection in `calibrate_rate_param` | proposal 6.4 compares at matched budget so a budget is what a config should name, and a rate parameter's usable range depends on the scale of the drive, which differs by encoder and by compression — E1 at the SPEC 4.2 default emits no events at all on audio
D51 | 2026-09-07 | Claude (impl remit) | R2 and every spiking condition are scored by one shared function, `harness.score_t1`, rather than by two paths meant to match | C4 requires identical decoding across conditions, and two copies could drift while each still looked right on its own; this is D30's argument for E2 and E3 sharing one lattice rule, applied to the decoder
D52 | 2026-09-07 | Claude (impl remit) | R2's analysis window is causal by default — it ends at the frame instant `t = k*hop` — with a `"centred"` option, and the choice is recorded with every result | proposal 5.9 fixes 40 bands, 25 ms and 10 ms but not the window phase, and a centred window sees 12.5 ms that the strictly causal kernel of equation (32) cannot, so a gap measured against it would be part encoding and part clairvoyance
D53 | 2026-09-07 | Claude (impl remit) | P1 runs at segment level, with the temporal and ceiling conditions producing a segment verdict by majority vote over the unchanged frame probe's per-frame predictions | equation (40)'s `n_c` is indexed by channel and nothing else so the representation can have no time axis, which is impossible on a frame grid; 4.1 already offers segment-level T1, and voting an unchanged probe avoids putting an invented pooling scheme inside the numerator
D54 | 2026-09-07 | Claude (impl remit) | P1's temporal condition is reported at its best `tau_phi` over the section 6.1 set {2, 5, 20} ms, not at a fixed value | 6.1 already requires it, and with `tau_phi` fixed at 5 ms the index changed sign at three of six budget points, because a count over a 60-140 ms segment against a 5 ms kernel compares integration windows as much as timing
D55 | 2026-09-07 | Claude (impl remit) | T3's peak suppression is strongest-first and its detection-to-reference matching is nearest-first, both one-to-one | the left-to-right alternatives make the output depend on the direction of time, which would bias a task whose entire subject is timing, and 4.3 requires neither
D56 | 2026-09-07 | Claude (impl remit) | T3's detection threshold is selected on the training split and applied unchanged to test, and every T3 result reports frame AUC beside its F-score | F-score at a single operating point needs a threshold and 4.3 names none, so choosing it on test would tune a free parameter against the reported number; and AUC is the probe alone where F-score is the probe plus threshold plus peak picker plus the corpus's boundary statistics, which on this corpus differ enough to reverse a conclusion
D57 | 2026-09-08 | SD | T2's headline correlation is the mean within-utterance Pearson r, with the pooled figure reported beside it, and the ridge regression is fitted in semitones | Q32/Q33; speakers differ in mean f0 far more than a contour moves within an utterance, so a pooled correlation is winnable by a predictor that emits one constant per utterance and would make T2 partly the speaker task D02 removed; and 4.2 reports RMSE in semitones, so fitting in hertz would leave estimator and metric disagreeing about what a good fit is
D58 | 2026-09-08 | Claude (impl remit) | Both probes drop zero-variance features rather than rescaling them to unit sigma | `featurise` leaves the OFF half at zero for a unipolar encoder by design, so half of every E1 feature vector is constant and mapping its sigma to 1.0 left all-zero columns that made `X'X` exactly singular at every operating point; measured effect on the recorded T1 sweep is five of six points bit-identical and one moving 0.0005, a fifth of one test frame
D59 | 2026-09-08 | Claude (impl remit) | T2's ridge penalty is swept and selected on speakers held out inside the training split, never on test, with the grid and chosen value recorded per condition | Q34; a single fixed penalty produced a prediction 468 octaves wide at the sparsest condition, and the same argument as T3's detection threshold applies — a free parameter tuned against the reported number is what the fairness constraints exist to prevent
D60 | 2026-09-08 | Claude (impl remit) | P2 is run and recorded as an explicit rehearsal, labelled in the script, the config and the result caveat, and is not treated as the week 4 gate | Q28's argument applies to P2 as much as to P1 — stationary phones and a linear f0 declination make the three tasks' demands weaker and more alike than on speech — and CLAUDE.md reserves battery questions to the design session
D61 | 2026-09-08 | Claude (impl remit) | Proposal 7.2's per-segment randomisation is added as `p2.randomise_times_in_segments` beside the SPEC section 7 operator rather than replacing it, and P2 runs both | Q35; `corrupt.randomise_times` is SPEC-defined and covered by a known-answer test, and the two operators support opposite conclusions about T1 — 0.1533 against 0.7656 — so which one P2 means has to be settled rather than chosen
D62 | 2026-09-08 | Claude (impl remit) | P2 holds each task's label alignment at its clean best across every corruption rather than re-selecting per condition | otherwise part of every measured degradation would be the alignment moving rather than information being destroyed, and Q24 established that alignment is worth 4.5 to 18.5 accuracy points
D67 | 2026-09-08 | SD+Claude | Equation (10)'s logarithmic branch becomes `log(1 + e/epsilon)`, amended in SPEC 3 and proposal 5 | Q23; the unshifted form is negative wherever the envelope is below unity, which is everywhere for a peak-normalised utterance, leaving E1 and E4 with no operating range between silence and saturation and inverting E6's gate, while E2 and E3 are indifferent because an additive offset cancels
D68 | 2026-09-08 | SD+Claude | E5's SPEC 4.6 default `cycle_divisor` becomes 1; the conftest registry operating point becomes 16 | Q19 and Q20; refractory caps the count near 7000 so a sweep centred on 4 spans 3.54x against D27's 4x, while a default of 4 also multiplied every ISI by four and broke test_T5_3 — the encoder's natural operating point and the gates' mid-range sweep point are two different numbers and now live in two files
D69 | 2026-09-08 | SD+Claude | The label alignment offset is a shared swept axis on the same footing as tau_phi, with the analytically predicted offset recorded beside the selected one | Q24; holding alignment at zero while sweeping tau_phi imposes a misalignment penalty that grows along the axis and then selects the tau_phi least harmed by it, which is the confound 6.1's own caveat exists to remove
D70 | 2026-09-08 | SD+Claude | Control C5 restated: sweep the offset over at least +/-2 frames and confirm an interior maximum at the selected offset, rather than confirm a drop at +/-1 | Q24; as written the control presumed zero was correct and would have been recorded as failed when the assumption underneath it was what had failed
D71 | 2026-09-08 | SD+Claude | Section 13 of the validation protocol: every free parameter is selected per condition on data held out within the training split, never on test, with grid and chosen value recorded; C4 reads as identical procedure rather than identical value | Q34; the same rule had been reached separately four times, and a fixed value across conditions differing by an order of magnitude in sparsity is the same number rather than the same treatment
D72 | 2026-09-08 | SD+Claude | T1 figures recorded before D69 are lower bounds and carry that note wherever cited, rather than being regenerated on that account | Q24; they are correct measurements at offset zero and the offset was not a declared axis when they were taken, so the record is accurate and only its interpretation changes
D73 | 2026-09-08 | SD+Claude | The validation protocol section added under D45 is renumbered from 8 to 12 and moved before the appendix, and the new free-parameter section is 13 | it was appended as section 8, colliding with the existing section 8 and landing after the appendix; D45 and the Q15 answer cite it as section 8 and the renumbered heading records that rather than leaving the references dangling

D74 | 2026-09-08 | Claude (impl remit) | Free-parameter selection is one mechanism, `spikeenc.selection`: speaker-disjoint K-fold inside the training split, one grid recorded per condition, K capped at the number of training speakers and set to that number on the stand-in corpus | D71 requires the procedure and not merely the intent, and four parameters chosen four ways is how three of them came to be chosen on test; K matters because at K=3 the inner fit set is four speakers of seven and T2's inner RMSE sat at the floor, 3.5 semitones against 1.35 at test, so the selection was being made by models that could not do the task
D75 | 2026-09-08 | Claude (impl remit) | Control C5's verdict is the interior-maximum test of D70 computed on the validation profile, with the test profile recorded beside it, and a plateau counts as a failure | the selected offset is defined by the validation sweep so that is the sweep an interior maximum is a property of; and a control that cannot separate the selected offset from its neighbour has not shown the alignment carries information, which is the only thing it is for
D76 | 2026-09-08 | Claude (impl remit) | T2's ridge penalty is selected on validation RMSE within each offset and the offset then on the headline correlation, both from one pass of fits; the RMSE-selected offset is recorded beside the chosen one | D59 fixes the penalty on RMSE because a scale-free correlation cannot see the 468-octave failure, while selecting the offset on RMSE and reporting a correlation would choose for one quantity and report another; the two disagree on this corpus and Q38 asks which is meant
D77 | 2026-09-08 | Claude (impl remit) | T1 gains the swept alignment axis on the same mechanism as T2, T3 and P1, defaulting to `offsets=(0,)` so the pinned reading is still reachable | D69 puts the offset on the same footing as tau_phi and names no task; D72 keeps the recorded T1 figures as lower bounds rather than errors, which is a statement about those numbers and not a reason to leave the axis unswept from here on
D78 | 2026-09-08 | Claude (impl remit) | Every task records `selection_bias`, the gap between the score at the validation-selected value and the score at the test argmax, alongside both profiles | the size of the bias D71 removes is a measurement this study is in a position to make rather than assert, and it is the only quantity that says whether the pre-D71 figures were slightly or badly flattered
D79 | 2026-09-08 | Claude (impl remit) | T3's detection threshold is swept on a coarser grid inside the selection folds than in the reported figure, a quarter of the points by default | the threshold is a nuisance parameter during selection, where the fold has only to rank the offsets, and it otherwise dominates the cost of the entire sweep because the peak picker re-runs once per grid point per fold per offset
D80 | 2026-09-09 | SD+Claude | Proposal sections 2, 5.2, 5.7, 9 and 10 corrected following a full reading of Tang et al. TETCI 2025: authorship, the shared gammatone front end, the intensity-to-place channel count, exact rate control, and the removal of the privacy-axis framing. D09 is not overturned; O3 is restated for Oliver | the earlier description was reconstructed from abstracts and was right about the algorithm but wrong about whose work it is, wrong that E7 does not share the front end, and wrong that reconstructibility constitutes a privacy gradient
D81 | 2026-09-09 | SD | Stage one starts on TIMIT as soon as the LDC copy is available; the open-corpus alternatives are not substituted for it, and Common Voice is retained only as a possible T2-only accent check | Q43; nothing in the study is pretrained, so the accent difference is the stage-two independent variable rather than a transfer risk, while neither LibriSpeech nor Common Voice carries hand-placed phone labels — which are T1's labels, T3's ground truth, and the calibration set for the forced aligner our own corpus will need, so dropping TIMIT would remove the calibration rather than the dependency, and would leave control C1 permanently unevaluable
D82 | 2026-09-10 | Claude (impl remit) | `calibrate_rate_param` searches integers when the encoder declares `RATE_PARAM_INTEGER`, and returns the nearest achievable rate rather than raising when no value falls within tolerance; the continuous path is untouched | D50 bisects in log space from a bracket starting at 1e-4, which E5 rejects before counting a single event because `cycle_divisor` must be a positive integer; and a discrete parameter's achievable rates are a countable set with gaps, so "within tol of the target" is not always satisfiable and the residual is a property of the encoder to be recorded rather than a failure to be raised
D83 | 2026-09-11 | Claude (impl remit) | E6's T1 sweep is recorded at four budget points, not six, with the top two absent rather than zero | E6's reachable rate over the standard bracket is [0, 3119.3] events/s at 32 channels, the `n_channels/hop` ceiling of Q46, so 6300 and 16000 have no E6 operating point to record, and a missing point and a bad point are different claims
D84 | 2026-09-11 | SD+Claude | Proposal 5.7 corrected against [Tang2025]: the 64-atom dictionary and 1920-channel figures are removed as unsourced, the "exact rate control" claim is retracted for the epsilon_min residual-energy stopping criterion, the logarithmic spacing of the characteristic intensities is recorded, and O3's third question is withdrawn since the authors' prototype answers it at K=3, M=40, 120 channels — inside our swept range. Validation protocol 12 gains a clause requiring every externally attributed figure to carry its location in the source | Q41 and Q42; the second of the two is a fabricated numeric parameter written with the confidence of read material, inside a section correcting an earlier confabulation, and it had reached a decision drafted for a collaborator before a second reader checked it
