# Corpus audio. TIMIT is LDC-licensed and redistribution is prohibited;
# the MANCHESTER Dataset is unreleased. This repository is public.
data/*
!data/.gitkeep
*.wav
*.flac
*.sph

# Design-session patch drops (see CLAUDE.md, GitHub conventions).
# Contents are committed on application, so the archive itself is redundant.
*.tar
*.tar.gz
*.tgz

__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store

results/**/*.npz
results/**/*.parquet
!results/manifest.json

# Design-session drops also arrive zipped (2026-09-06).
*.zip

# Published papers. Licensed, and this repository is public (see
# docs/references.md). Only this directory — report PDFs under reports/ are
# ours and are committed.
papers/
