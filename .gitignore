# Corpus audio. TIMIT is LDC-licensed and redistribution is prohibited;
# the MANCHESTER Dataset is unreleased. This repository is public.
data/*
!data/.gitkeep
*.wav
*.flac
*.sph

# Design-session patch drops (see CLAUDE.md, GitHub conventions).
# Contents are committed on application, so the archive itself is redundant.
*.tar
*.tar.gz
*.tgz

__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store

results/**/*.npz
results/**/*.parquet
!results/manifest.json
